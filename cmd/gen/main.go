// Command gen раскрывает правила из data/rules на диапазон дат и пишет
// готовые .ics-фиды в выходной каталог.
//
// Реализация появится на Этапе 1 (см. PLAN.md). Сейчас это каркас.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "gen: не реализовано — см. PLAN.md, Этап 1")
	os.Exit(1)
}
