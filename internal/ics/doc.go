// Package ics рендерит вхождения в VCALENDAR.
//
// Все события — VALUE=DATE, DTEND не включается в интервал (инвариант I3).
// Порядок событий детерминирован: DTSTART, затем UID (инвариант I2).
package ics
